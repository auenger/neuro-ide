[
  {
    "id": "architect",
    "name": "MEPER_Page_DEV",
    "icon": "user",
    "prompt": "# MEPER 平台页面开发模式详解\n\nMEPER平台提供两种主要的页面开发模式，开发者可以根据项目需求和复杂程度选择适合的开发方式。\n\n## 模式一：传统Vaadin Java代码开发\n\n### 概述\n这种方式遵循传统的Vaadin开发模式，通过Java代码直接构建页面组件和布局。开发者继承平台提供的基础视图类，使用Java代码编写页面逻辑和UI。\n\n### 基础类继承关系\n\n#### PC端页面\n- **基类**: `com.ags.meperframework.vaadin.core.BasePcView`\n  - 继承自: `com.ags.meperframework.vaadin.core.BaseView`\n  - 用途: 用于开发桌面端页面\n\n#### 移动端页面\n- **基类**: `com.ags.meperframework.vaadin.core.BaseMobileView`\n  - 继承自: `com.ags.meperframework.vaadin.core.BaseView`\n  - 用途: 用于开发移动端页面\n\n### BaseView类结构分析\n\n**类路径**: `com.ags.meperframework.vaadin.core.BaseView`\n\nBaseView是MEPER平台所有页面视图的基类，它继承了Vaadin的Composite类并实现了IBaseView接口。\n\n#### 类定义\n```java\npublic abstract class BaseView extends BaseComponent implements IBaseView, ErrorHandler {\n```\n\n#### 核心字段\n- `path` (`String`): 页面路径\n- `menuNodeWrapper` (`MenuNodeWrapper`): 菜单节点包装器\n- `logger` (`Logger`): 日志记录器\n\n#### 核心方法类型\n\n##### 1. 生命周期方法\n```java\n// 页面初始化入口\npublic void init()\n\n// 页面离开之前执行\npublic void beforeLeave(BeforeLeaveEvent event)\n\n// 页面进入之前执行\npublic void beforeEnter(BeforeEnterEvent event)\n\n// 导航完成后执行\npublic void afterNavigation(AfterNavigationEvent event)\n```\n\n##### 2. 抽象方法（必须实现）\n```java\n// 获取菜单树工厂，用于确定页面菜单配置\nprotected abstract MenuTreeFactory getMenuTreeFactory()\n\n// 初始化页面布局，添加页面元素\nprotected abstract void initView()\n\n// 页面初始化后，进行数据加载\nprotected abstract void loadData()\n```\n\n##### 3. 错误处理方法\n```java\n// 错误处理\npublic void error(ErrorEvent event)\n```\n\n### BasePcView类结构分析\n\n**类路径**: `com.ags.meperframework.vaadin.core.BasePcView`\n\nBasePcView继承了BaseView，为PC端页面提供专门的菜单树工厂。\n\n```java\npublic abstract class BasePcView extends BaseView {\n\n    @Override\n    protected MenuTreeFactory getMenuTreeFactory() {\n        return PcMenuTreeFactory.getInstance();\n    }\n}\n```\n\n### 页面实现类模板\n\n以下是创建MEPER平台页面的标准模板：\n\n```java\npackage com.ags.meperframework.suites.appbase.vaadin.ui.pc.view;\n\nimport com.ags.meperframework.core.i18n.I18NUtility;\nimport com.ags.meperframework.core.spring.BeanManager;\nimport com.ags.meperframework.suites.appbase.sdk.domain.MyEntity;  // 替换为实际实体\nimport com.ags.meperframework.suites.appbase.sdk.service.api.IMyEntityService; // 替换为实际Service\nimport com.ags.meperframework.suites.appbase.vaadin.component.paginationobjectlist.IDomainObjectGrid;\nimport com.ags.meperframework.suites.appbase.vaadin.component.paginationobjectlist.PaginationDomainObjectList;\nimport com.ags.meperframework.suites.appbase.vaadin.component.slidedialog.SlideDialog;\nimport com.ags.meperframework.vaadin.core.*;\nimport com.ags.meperframework.vaadin.util.NotificationUtils;\nimport com.ags.meperframework.web.i18n.I18Support;\nimport com.vaadin.flow.component.ClickEvent;\nimport com.vaadin.flow.component.Component;\nimport com.vaadin.flow.component.ComponentEventListener;\nimport com.vaadin.flow.component.button.Button;\nimport com.vaadin.flow.component.button.ButtonVariant;\nimport com.vaadin.flow.component.grid.GridVariant;\nimport com.vaadin.flow.component.icon.Icon;\nimport com.vaadin.flow.component.icon.VaadinIcon;\nimport com.vaadin.flow.component.orderedlayout.VerticalLayout;\nimport com.vaadin.flow.spring.annotation.SpringComponent;\nimport com.vaadin.flow.spring.annotation.UIScope;\n\nimport jakarta.inject.Inject;\n\nimport java.util.List;\n\n/**\n * MyEntity管理页面实现 - 替换为实际实体名称\n * 继承BasePcView，用于PC端页面开发\n */\n@SpringComponent  // 标记为Spring组件\n@UIScope          // UI作用域，每次UI实例化都会创建新实例\npublic class MyEntityViewImpl extends BasePcView  // 继承BasePcView用于PC端，BaseMobileView用于移动端\n        implements ComponentEventListener<ClickEvent<Button>>, IMyEntityView { // 实现组件事件监听器和业务视图接口\n\n    private static final long serialVersionUID = 1L; // 替换为实际序列号\n\n    // 滑动对话框组件，用于显示详情或编辑页面\n    private SlideDialog slideDialog = new SlideDialog();\n\n    // 页面呈现器，用于处理业务逻辑\n    @Inject\n    private MyEntityViewPresenter presenter; // 替换为实际Presenter\n\n    // 页面操作按钮\n    @I18Support(caption = \"Add\", captionKey = \"common.add\")\n    private Button btnAdd = new Button();\n\n    @I18Support(caption = \"Delete\", captionKey = \"common.delete\") \n    private Button btnDelete = new Button();\n\n    @I18Support(caption = \"Refresh\", captionKey = \"common.refresh\")\n    private Button btnRefresh = new Button();\n\n    private Button btnSearch = new Button();\n    private Button[] btns = {btnAdd, btnDelete, btnRefresh};\n\n    // 数据表格组件，用于显示MyEntity列表\n    private IDomainObjectGrid<MyEntity> objectGrid = new PaginationDomainObjectList<>();\n\n    @Inject\n    private IAddMyEntityDialog addMyEntityDialog; // 替换为实际对话框接口\n\n    // 构造函数：初始化页面组件和布局\n    public MyEntityViewImpl() {\n        // 创建主布局\n        VerticalLayout verticalLayout = new VerticalLayout();\n        verticalLayout.setSizeFull();\n        verticalLayout.setPadding(false);\n\n        vlObjectGrid.setSizeFull();\n\n        // 添加操作按钮并注册事件监听器\n        for (Button btn : btns) {\n            vlObjectGrid.addButton(btn);\n            btn.addClickListener(this); // 绑定事件监听器\n            btn.setDisableOnClick(true); // 防止重复点击\n        }\n        \n        // 设置按钮ID以便识别和自动化测试\n        setButtonIds();\n        \n        // 设置按钮主题和图标\n        btnAdd.addThemeVariants(ButtonVariant.LUMO_PRIMARY);\n        btnDelete.addThemeName(ThemeVariant.BUTTON_DELETE);\n        btnAdd.setIcon(new Icon(VaadinIcon.PLUS));\n        btnDelete.setIcon(new Icon(VaadinIcon.TRASH));\n        btnRefresh.setIcon(new Icon(VaadinIcon.REFRESH));\n        btnSearch.setIcon(new Icon(VaadinIcon.LIST));\n\n        // 配置表格组件\n        objectGrid.addThemeVariants(GridVariant.LUMO_NO_BORDER);\n        objectGrid.setAllRowsVisible(true);\n        \n        // 添加表格点击事件监听器\n        objectGrid.getGrid().addItemClickListener(event -> {\n            MyEntity selectedObject = objectGrid.getSelectedObject(); // 替换为实际对象类型\n            if (selectedObject != null) {\n                slideDialog.setObject(selectedObject);\n                slideDialog.showPopup(SlideModelEnum.VIEW); // 显示查看模式\n            } else {\n                slideDialog.setObject(null);\n                slideDialog.closePopup();\n            }\n            setButtonStatus(selectedObject);\n        });\n\n        // 设置面板主题并添加内容\n        vlObjectGrid.addThemeVariant(PanelContentVariant.HEIGHT_AUTO);\n        vlObjectGrid.setContent((Component) objectGrid);\n        verticalLayout.add(vlObjectGrid);\n\n        // 添加滑动对话框\n        verticalLayout.add(slideDialog);\n        \n        // 设置页面尺寸和根组件\n        // 强制注意下 这里是必要的必须要给页面设置根节点组件用下面两句代码\n        this.setSizeFull();\n        this.setCompositionRoot(verticalLayout);\n    }\n\n    // 设置按钮ID\n    private void setButtonIds() {\n        btnAdd.setId(\"btn_add_myentity\"); // 替换为实际ID\n        btnDelete.setId(\"btn_delete_myentity\");\n        btnRefresh.setId(\"btn_refresh_myentity\");\n    }\n\n    /**\n     * 初始化视图方法\n     * 在页面组件初始化完成后调用\n     * 用于配置表格列、数据源、滑动面板内容等\n     */\n    @Override\n    protected void initView() {\n        // 添加滑动对话框组件到侧滑面板\n        slideDialog.add(BeanManager.getService(MyEntityPropertiesSlide.class).setSlideDataChange(cr -> { \n            objectGrid.refresh();\n            slideDialog.setObject(cr.getObj());\n            slideDialog.setModel(SlideModelEnum.EDIT);\n        }));\n        slideDialog.add(BeanManager.getService(MyEntityValueSlide.class)); // 添加值滑动组件\n        \n        // 设置页面呈现器视图\n        presenter.setView(this);\n        \n        // 配置数据源\n        objectGrid.setServiceClass(IMyEntityService.class); // 替换为实际Service接口\n        objectGrid.clearSortColumnName();\n        objectGrid.setSort(MyEntityEntity.NAME); // 替换为实际排序字段\n\n        // 添加表格列，根据实际实体属性配置\n        objectGrid.addColumn(MyEntity::getName) // 替换为实际getter方法\n                .setSortOrderProvider(myEntity -> {\n                    List<QuerySortOrder> list = new ArrayList<>();\n                    list.add(new QuerySortOrder(MyEntityEntity.NAME, myEntity)); // 替换为实际字段\n                    return list.stream();\n                }).setHeader(I18NUtility.getValue(\"common.name\", \"Name\")); // 替换为实际国际化键值\n        \n        objectGrid.attachFixedColumns(true);\n        setButtonStatus(null);\n    }\n\n    /**\n     * 数据加载方法\n     * 页面初始化后自动调用，用于加载数据\n     * 实现IBaseView接口的抽象方法\n     */\n    @Override\n    protected void loadData() {\n        objectGrid.refresh();\n        setButtonStatus(null);\n    }\n\n    // 设置按钮状态，根据选择的对象启用/禁用按钮\n    private void setButtonStatus(MyEntity optional) { // 替换为实际对象类型\n        boolean enabled = (optional != null);\n        if (enabled) {\n            // 根据业务逻辑设置某些按钮的可用性\n            if (optional.getName().equals(MyEntityConstant.SYSTEM_MYENTITY_NAME)) { // 替换为实际常量\n                btnDelete.setEnabled(false);\n            } else {\n                btnDelete.setEnabled(true);\n            }\n        } else {\n            btnDelete.setEnabled(enabled);\n        }\n        btnAdd.setEnabled(true);\n        btnRefresh.setEnabled(true);\n    }\n\n    /**\n     * 处理按钮点击事件\n     * 实现ComponentEventListener接口\n     */\n    @Override\n    public void onComponentEvent(ClickEvent<Button> event) {\n        Button clickedButton = event.getSource();\n        clickedButton.setEnabled(true); // 点击后重新启用按钮\n        \n        if (clickedButton.equals(btnAdd)) {\n            // 处理添加按钮点击\n            slideDialog.showPopup(SlideModelEnum.ADD); // 显示添加模式\n        } else if (clickedButton.equals(btnDelete)) {\n            // 处理删除按钮点击\n            ConfirmDialog.show(\n                I18NUtility.getValue(\"common.suretodelete\", \"Are you sure to delete the selected item?\"),\n                result -> {\n                    if (result.getResult().equals(ConfirmResult.Result.OK)) {\n                        try {\n                            presenter.deleteMyEntity(objectGrid.getSelectedObject()); // 替换为实际删除方法\n                        } catch (RelationshipCheckFailedException e) { // 替换为实际异常类型\n                            NotificationUtils.notificationError(\n                                    I18NUtility.getValue(\"Common.RelationShipCheckFailed\", e.getLocalizedMessage()));\n                        } catch (Exception e) {\n                            NotificationUtils.notificationException(e);\n                        }\n                        objectGrid.refresh();\n                        setButtonStatus(null);\n                        slideDialog.closePopup();\n                    }\n                });\n        } else if (clickedButton.equals(btnRefresh)) {\n            // 处理刷新按钮点击\n            objectGrid.refresh();\n            setButtonStatus(null);\n        }\n    }\n\n    /**\n     * 获取页面标题 就是设置当前页面的名称 类似Dialog的 Caption\n     * 实现HasDynamicTitle接口的方法\n     */\n    @Override\n    public String getPageTitle() {\n        return I18NUtility.getValue(\"admin.myentity\", \"My Entity\"); // 替换为实际国际化键值\n    }\n}\n```\n\n### 实现类必须遵循的规则\n\n1. **继承规则**：\n   - PC端页面继承 `BasePcView`\n   - 移动端页面继承 `BaseMobileView`\n\n2. **抽象方法实现**：\n   - `initView()`: 初始化页面布局和组件配置\n   - `loadData()`: 加载页面数据\n   - `getPageTitle()`: 获取页面标题（如果实现IBaseView接口）\n\n3. **Spring注解**：\n   - 使用 `@SpringComponent` 标记为Spring组件\n   - 使用 `@UIScope` 确保每次UI实例化都创建新实例\n\n4. **依赖注入**：\n   - 使用 `@Inject` 注入Presenter和其他依赖\n   - 使用 `BeanManager.getService()` 获取其他服务\n\n5. **国际化支持**：\n   - 使用 `@I18Support` 注解标记需要国际化的组件属性\n   - 使用 `I18NUtility.getValue()` 方法获取国际化文本\n\n6. **生命周期方法**：\n   - `init()`: 页面初始化入口\n   - `beforeEnter()`: 页面进入前处理\n   - `beforeLeave()`: 页面离开前处理\n\n## 模式二：PageEditor可视化开发\n\n### 概述\n这种方式使用MEPER平台内置的PageEditor，在运行时动态渲染Vaadin页面。开发者通过可视化工具设计页面布局，然后通过ViewModel实现业务逻辑。\n\n### 基础类继承关系\n\n- **基类**: `com.ags.meperframework.suites.appbase.vaadin.designer.runtime.BaseViewModel`\n  - 用于实现页面逻辑的ViewModel基类\n\n### ViewModel模板\n\n```java\npackage com.ags.meperframework.suites.appbase.vaadin.ui.admin.viewmodel;\n\nimport com.ags.meperframework.core.exception.PlatformException;\nimport com.ags.meperframework.core.spring.BeanManager;\nimport com.ags.meperframework.suites.appbase.sdk.domain.MyEntity; // 替换为实际实体\nimport com.ags.meperframework.suites.appbase.sdk.service.api.IMyEntityService; // 替换为实际Service\nimport com.ags.meperframework.suites.appbase.vaadin.component.lumos.LumosButton;\nimport com.ags.meperframework.suites.appbase.vaadin.component.paginationobjectlist.PaginationObjectListGrid;\nimport com.ags.meperframework.suites.appbase.vaadin.designer.runtime.BaseViewModel;\nimport com.ags.meperframework.suites.appbase.vaadin.designer.runtime.Widget;\nimport com.ags.meperframework.vaadin.core.ConfirmResult;\n\n/**\n * MyEntity页面的ViewModel\n * 通过PageEditor可视化设计页面，使用此ViewModel实现逻辑\n */\npublic class MyEntityViewModel extends BaseViewModel {\n\n    // 使用@Widget注解绑定页面上的组件，组件名为\"myEntityGrid\"\n    @Widget(\"myEntityGrid\")\n    private PaginationObjectListGrid<MyEntity> myEntityGrid;\n\n    // 绑定页面上的添加按钮\n    @Widget(\"btnAdd\")\n    private LumosButton btnAdd;\n\n    // 绑定页面上的删除按钮\n    @Widget(\"btnDelete\")\n    private LumosButton btnDelete;\n\n    // 绑定页面上的编辑按钮\n    @Widget(\"btnEdit\")\n    private LumosButton btnEdit;\n\n    /**\n     * 页面初始化后，自动会被调用\n     * 用于设置事件监听器和初始化数据\n     */\n    @Override\n    public void init() {\n        // 初始化组件状态\n        btnEdit.setVisible(false);\n        \n        // 添加按钮点击事件处理\n        btnAdd.addClickListener(event -> {\n            // 创建添加实体对话框\n            AddMyEntityDialog addDialog = new AddMyEntityDialog();\n            addDialog.setData(null);\n            addDialog.initData();\n            \n            // 显示对话框并处理结果\n            addDialog.show(result -> {\n                if (result.getResult().equals(ConfirmResult.Result.OK)) {\n                    myEntityGrid.refresh();\n                }\n            });\n        });\n\n        // 删除按钮点击事件处理\n        btnDelete.addClickListener(event -> {\n            MyEntity selectedEntity = myEntityGrid.getSelectedObject();\n            if (selectedEntity == null) {\n                throw new PlatformException(\"请先选择一条记录\");\n            }\n            \n            // 调用服务删除数据\n            BeanManager.getService(IMyEntityService.class).delete(selectedEntity);\n            \n            // 刷新表格\n            myEntityGrid.refresh();\n        });\n\n        // 编辑按钮点击事件处理\n        btnEdit.addClickListener(event -> {\n            MyEntity selectedEntity = myEntityGrid.getSelectedObject();\n            if (selectedEntity == null) {\n                throw new PlatformException(\"请先选择一条记录\");\n            }\n        });\n\n        // 表格点击事件处理\n        myEntityGrid.getGrid().addItemClickListener(event -> {\n            MyEntity entity = event.getItem();\n            if (entity != null) {\n                // 根据选择设置按钮状态\n                setButtonStatus(entity);\n            }\n        });\n    }\n    \n    // 设置按钮状态的方法\n    private void setButtonStatus(MyEntity entity) {\n        boolean enabled = entity != null;\n        btnDelete.setEnabled(enabled);\n        btnEdit.setEnabled(enabled && !entity.isSystemEntity()); // 根据业务逻辑设置\n    }\n}\n```\n\n## 两种开发模式对比\n\n| 特性 | 传统Java代码开发 | PageEditor可视化开发 |\n|------|------------------|---------------------|\n| **开发方式** | 编写Java代码 | 可视化拖拽设计 |\n| **灵活性** | 高度灵活 | 标准化页面 |\n| **复杂度处理** | 适合复杂页面 | 适合标准页面 |\n| **维护成本** | 较高 | 较低 |\n| **学习成本** | 中等 | 需学习PageEditor工具 |\n| **性能** | 通常更好 | 有轻微运行时开销 |\n| **调试能力** | 便于调试 | 调试较困难 |\n| **适用场景** | 复杂逻辑、特殊需求 | 标准业务页面 |\n\n## 最佳实践\n\n### 传统Java代码开发最佳实践\n1. 在构造函数中配置页面布局和事件监听器\n2. 在initView()方法中配置数据源和表格列\n3. 在loadData()方法中加载初始数据\n4. 使用BeanManager获取服务实例\n5. 遵循异常处理和国际化规范\n\n### PageEditor可视化开发最佳实践\n1. 使用PageEditor设计页面布局\n2. 在ViewModel中使用@Widget注解绑定组件\n3. 在init()方法中实现页面逻辑\n4. 合理使用数据绑定和事件处理\n5. 注意页面性能优化",
    "customPrompt": "[架构师]$ ",
    "isBuiltIn": true,
    "isActive": true
  },
  {
    "id": "frontend",
    "name": "前端工程师",
    "icon": "monitor",
    "prompt": "# 前端工程师角色\n\n你是一个前端开发专家,负责:\n- UI/UX 实现\n- 前端框架开发\n- 性能优化\n- 响应式设计",
    "customPrompt": "[前端]$ ",
    "isBuiltIn": true,
    "isActive": true
  },
  {
    "id": "backend",
    "name": "后端工程师",
    "icon": "server",
    "prompt": "# 后端工程师角色\n\n你是一个后端开发专家,负责:\n- API 设计与实现\n- 数据库设计\n- 服务器架构\n- 安全与性能",
    "customPrompt": "[后端]$ ",
    "isBuiltIn": true,
    "isActive": true
  },
  {
    "id": "role-1768295786295",
    "name": "测试",
    "icon": "cloud",
    "prompt": "# 新角色\n\n这是新角色的系统提示词。",
    "customPrompt": "[测试]$ ",
    "isBuiltIn": false,
    "isActive": true
  }
]